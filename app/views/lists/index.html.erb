<% content_for :title, "Lists" %>

<h2 class="font-serif text-lg font-semibold tracking-tight text-neutral-900">Lists</h2>

<div id="lists">
  <% @lists.each do |list| %>
      <%= render list %>
  <% end %>
</div>
